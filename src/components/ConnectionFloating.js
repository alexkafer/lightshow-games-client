import React from 'react'

import ConnectionStatus from './ConnectionStatus'

const ConnectionFloating = () => (
      <div className="connection-floating">
        <ConnectionStatus />
      </div>
  );

export default ConnectionFloating